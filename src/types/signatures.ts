export type FormData = {
    name: string
    title: string
    company: string
    email: string
    phone: string
    website: string
    linkedin: string
    twitter: string
    location: string
    image: string 
  }
  
  export type SignatureProps = {
    formData: FormData
  }
  
  